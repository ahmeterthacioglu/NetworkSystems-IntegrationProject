package client;

public enum MessageType {
    FREE,
    BUSY,
    DATA,
    SENDING,
    ACK,
    DONE_SENDING,
    DATA_SHORT,
    END,
    HELLO,
    TOKEN_ACCEPTED,
    HEARTBEAT, PRESENCE, TOKEN_REJECTED

}